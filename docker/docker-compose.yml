version: "3"
services:
  entity:
    image: entity
    container_name: nt2
    build:
      context: .
      args:
        USERNAME: $USER
    volumes:
      - type: bind
        source: ../
        target: /home/$USER/entity
